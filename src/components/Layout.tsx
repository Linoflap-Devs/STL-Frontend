// components/Layout.js
import Sidebar from './Sidebar';

const Layout = ({  }) => {
  return (
    <div className="layout">
      <Sidebar />
      <main>{}</main>
    </div>
  );
};

export default Layout;
